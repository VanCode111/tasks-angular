import { PinkTextDirective } from './pink-text.directive';

describe('PinkTextDirective', () => {
  it('should create an instance', () => {
    const directive = new PinkTextDirective();
    expect(directive).toBeTruthy();
  });
});

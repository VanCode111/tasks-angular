import { CamelTextPipe } from './camel-text.pipe';

describe('CamelTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CamelTextPipe();
    expect(pipe).toBeTruthy();
  });
});
